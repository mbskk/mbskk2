pieline{
  agent any 
  stages{
    stage('Build'){
      steps{
        echo "Example Deploy"
      }
    }
      stage('Deploy to main'){
        when {
          branch 'main'
          environment name:'branch', value:'main'
        }
        steps{
          echo "deploy to main"
        }
      }
  }
}
